# 🚀 02 – Funções e Estruturas em C

Nesta etapa do meu aprendizado em C, estou evoluindo para estruturas mais robustas e profissionais da linguagem.  

Aqui estudo:

- **Funções**
- **Arrays (vetores e matrizes)**
- **Strings**
- **Structs**

Cada subpasta contém exercícios práticos criados para fortalecer minha lógica e construir programas mais organizados e reutilizáveis.

---

## 📌 **1. Funções**
Programas focados em modularização, reutilização de código e clareza.

📂 *Arquivos:*
- `soma-funcao.c` – função simples que soma dois números  
- `fatorial-recursivo.c` – uso de recursão  
- `calculadora-funcoes.c` – cada operação é uma função  
- `conversor-temperatura.c` – funções para conversão entre escalas  

---

## 📌 **2. Arrays**
Manipulação de vetores e matrizes.

📂 *Arquivos:*
- `notas-array.c` – exibe a nota de alunos utilizando matrizes  
- `maior-menor-array.c` – encontra o maior e menor valor do vetor  
- `media-array.c` – calcula média de elementos  
- `matriz-soma.c` – soma de duas matrizes 5x5  
- `busca-linear.c` – pesquisa por elemento no vetor  

---

## 📌 **3. Strings**
Manipulação de texto com funções da `<string.h>` e lógica pura.

📂 *Arquivos:*
- `conta-caracteres.c` – conta comprimento da string  
- `compara-strings.c` – comparação manual e com `strcmp()`  
- `inverte-string.c` – inverte os caracteres manualmente  
- `valida-palindromo.c` – identifica se a palavra é palíndromo  

---

## 📌 **4. Structs**
Introdução à criação de tipos de dados personalizados.

📂 *Arquivos:*
- `cadastro-pessoa.c` – struct com nome, idade e cidade  
- `cadastro-aluno.c` – struct + cálculo de média  
- `produto-estoque.c` – manipulação de array de structs  
- `livro-biblioteca.c` – catálogo simples de livros  

---

## 🛠️ **Como compilar os programas**

```bash
gcc nome-do-arquivo.c -o programa
./programa
